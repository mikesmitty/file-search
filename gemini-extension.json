{
  "name": "file-search-extension",
  "version": "0.1.3",
  "mcpServers": {
    "file-search": {
      "command": "${extensionPath}${/}file-search",
      "args": [
        "mcp"
      ],
      "cwd": "${workspacePath}"
    }
  }
}